const pi = 3.1415962;

const doublePi = () => {
    return pi * 2;
};

const triplePi = () => {
    return pi * 3;
};

export default pi;
// to export another files
export { doublePi, triplePi };
